version: '3.8'

services:
  stock-market-analysis-app:
    build: .
    image: stock-market-analysis:latest
    container_name: stock-market-analysis-webapp
    ports:
      - "3000:3000"
    volumes:
      # Mount database for persistence (read-write for data fetching)
      - ./database:/app/database
      # Mount logs directory if needed
      - ./logs:/app/logs
    environment:
      - NODE_ENV=production
      - PORT=3000
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "node", "-e", "require('http').get('http://localhost:3000/api/health', (res) => { process.exit(res.statusCode === 200 ? 0 : 1) })"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    networks:
      - stock-market-analysis-network

  # Development service (optional)
  stock-market-analysis-dev:
    build:
      context: .
      dockerfile: Dockerfile
      target: build
    container_name: stock-market-analysis-dev
    ports:
      - "3001:3000"
      - "5173:5173"
    volumes:
      # Mount source code for hot reloading
      - .:/app
      - /app/node_modules
      - /app/backend/node_modules
      - /app/frontend/node_modules
      - ./database:/app/database
    environment:
      - NODE_ENV=development
      - PORT=3000
    command: npm run dev
    profiles:
      - dev
    networks:
      - stock-market-analysis-network

networks:
  stock-market-analysis-network:
    driver: bridge

volumes:
  logs:
